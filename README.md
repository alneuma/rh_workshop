# Repo for the Workshop about RedHead Academy at 42 Berlin

`vm_setup.md` contains a small guide for setting up a virtual machine with Rocky Linux. The virtual machine can be used later for experimentation and learning.

`vbox_commands.md` contains a small cheatsheet with useful commands for managing your virtual machine from the command line. You might want to use it while working through `vm_setup.md`.
